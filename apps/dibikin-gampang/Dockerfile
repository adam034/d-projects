# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build dibikin-gampang`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t dibikin-gampang`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system dibikin-gampang && \
          adduser --system -G dibikin-gampang dibikin-gampang

COPY dist/apps/dibikin-gampang dibikin-gampang
RUN chown -R dibikin-gampang:dibikin-gampang .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix dibikin-gampang --omit=dev -f install

CMD [ "node", "dibikin-gampang" ]
